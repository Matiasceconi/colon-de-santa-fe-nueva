import json
from pathlib import Path
fixes=json.loads("[{\"path\":\"src/pages/MicrocycleHistory.jsx\",\"old\":\"Archive, CheckCircle2, \",\"new\":\"\"},{\"path\":\"src/components/schedule/dailySchedulePdf.js\",\"old\":\"  // Precargar escudos de rivales\\n  const matchEvents = events.filter(e => normalize(`${e.event_type} ${e.type} ${e.title}`).includes(\\\"partido\\\"));\\n  const rivalLogos = await Promise.all(matchEvents.map(e => loadSafeImage(e.rival_logo_url)));\\n  setFill(doc, \\\"#F7F7F5\\\"); doc.rect(0, 0, 210, 297, \\\"F\\\");\\n  drawHeader(doc, day, squadName, logo, brand);\\n  const sorted = [...events].sort((a, b) => (a.time || a.start_time || \\\"\\\").localeCompare(b.time || b.start_time || \\\"\\\"));\\n  drawTable(doc, sorted, brand);\\n  // Dibujar escudos de partidos sobre la sección de partido\\n  matchEvents.forEach((ev, i) => {\\n    const logo = rivalLogos[i];\\n    if (logo) {\\n      const matchY = 66 + 14 + Math.max(20, Math.min(38, 185 / Math.max(sorted.filter(e => !normalize(`${e.event_type} ${e.type} ${e.title}`).includes(\\\"partido\\\")).length, 1))) * sorted.filter(e => !normalize(`${e.event_type} ${e.type} ${e.title}`).includes(\\\"partido\\\")).length + 6 + i * 64;\\n      addImageSafe(doc, logo, 7 + 196 / 2 - 12, matchY + 12, 24, 24);\\n    }\\n  });\\n  drawFooter(doc, logo, brand);\\n\",\"new\":\"  const sorted=[...events].sort((a,b)=>(a.time||a.start_time||\\\"\\\").localeCompare(b.time||b.start_time||\\\"\\\"));\\n  const isMatch=e=>normalize(`${e.event_type} ${e.type} ${e.title}`).includes(\\\"partido\\\");\\n  const pages=[];let group=[];\\n  for(const event of sorted){\\n    const next=[...group,event],matches=next.filter(isMatch).length,others=next.length-matches;\\n    if(group.length && 20+Math.max(others,1)*38+matches*64>186){pages.push(group);group=[];}\\n    group.push(event);\\n  }\\n  if(group.length||!pages.length)pages.push(group);\\n  for(let index=0;index<pages.length;index++){\\n    if(index)doc.addPage();\\n    setFill(doc,\\\"#F7F7F5\\\");doc.rect(0,0,210,297,\\\"F\\\");\\n    drawHeader(doc,day,squadName,logo,brand);\\n    drawTable(doc,pages[index],brand);\\n    const matches=pages[index].filter(isMatch),otherCount=pages[index].length-matches.length;\\n    for(let i=0;i<matches.length;i++){\\n      const rivalLogo=await loadSafeImage(matches[i].rival_logo_url);\\n      const matchY=66+14+38*Math.max(otherCount,1)+6+i*64;\\n      if(rivalLogo)addImageSafe(doc,rivalLogo,93,matchY+12,24,24);\\n    }\\n    drawFooter(doc,logo,brand);\\n  }\\n\"},{\"path\":\"src/components/schedule/dailySchedulePdf.js\",\"old\":\"setText(doc, \\\"#FFFFFF\\\")\",\"new\":\"setText(doc, contrastText(colors.primaryDeep))\",\"all\":true},{\"path\":\"src/components/schedule/dailySchedulePdf.js\",\"old\":\"setText(doc, colors.primaryDeep); doc.setFont(\\\"helvetica\\\", \\\"bold\\\"); doc.setFontSize(10.5);\",\"new\":\"setText(doc, contrastText(colors.accent)); doc.setFont(\\\"helvetica\\\", \\\"bold\\\"); doc.setFontSize(10.5);\"},{\"path\":\"src/components/sessions/SessionDayMap.jsx\",\"old\":\"const { clubBrand } = useWorkspace();\",\"new\":\"const { clubBrand, can } = useWorkspace();\"},{\"path\":\"src/components/sessions/SessionDayMap.jsx\",\"old\":\"import { useWorkspace }\",\"new\":\"import { createBrandedPdf, buildExportFileName } from \\\"@/lib/exports/pdfExportKit\\\";\\nimport { assertExportAllowed } from \\\"@/lib/exports/exportSecurity\\\";\\nimport { useWorkspace }\"},{\"path\":\"src/components/sessions/SessionDayMap.jsx\",\"old\":\"    try {\\n      const svg\",\"new\":\"    try {\\n      assertExportAllowed(can,\\\"/sessions\\\");\\n      const svg\"},{\"path\":\"src/components/sessions/SessionDayMap.jsx\",\"old\":\"        const { jsPDF } = await import(\\\"jspdf\\\");\\n        const pdf = new jsPDF({ orientation:\\\"portrait\\\", unit:\\\"mm\\\", format:\\\"a4\\\" });\\n        const width = Math.min(190, 277 * 960 / map.height);\\n        const height = width * map.height / 960;\\n        pdf.addImage(canvas.toDataURL(\\\"image/png\\\"), \\\"PNG\\\", (210-width)/2, 10, width, height);\\n        pdf.save(filename + \\\".pdf\\\");\\n\",\"new\":\"        const pdf=await createBrandedPdf({brand:clubBrand,title:\\\"Mapa del día\\\",subtitle:label});\\n        pdf.image(canvas.toDataURL(\\\"image/png\\\"),{width:186,height:Math.min(205,186*map.height/960)});\\n        pdf.sectionTitle(\\\"Detalle completo de grupos\\\");\\n        for(const [name,group] of [...Object.entries(map.groups),[\\\"Sin posición\\\",map.unknown],[\\\"Diferenciado\\\",map.diferenciados],[\\\"Kinesiología\\\",map.kinesiologia]]){\\n          if(!group?.length)continue;\\n          pdf.sectionTitle(name);\\n          pdf.table({columns:[{key:\\\"player_name\\\",label:\\\"Jugador\\\",value:p=>p.player_name||p.full_name||\\\"Jugador\\\"},{key:\\\"position\\\",label:\\\"Posición\\\"}],rows:group});\\n        }\\n        pdf.finalize().save(buildExportFileName(\\\"Mapa_del_dia\\\",[clubBrand?.name,session.squad_name,session.date]));\\n\"},{\"path\":\"src/components/performance/MinutesSubPanel.jsx\",\"old\":\"import { generateMinutesPdf }\",\"new\":\"import { assertExportAllowed } from \\\"@/lib/exports/exportSecurity\\\";\\nimport { useToast } from \\\"@/components/ui/use-toast\\\";\\nimport { generateMinutesPdf }\"},{\"path\":\"src/components/performance/MinutesSubPanel.jsx\",\"old\":\"  const dashboard = useMinutesDashboard();\",\"new\":\"  const dashboard = useMinutesDashboard();\\n  const {toast}=useToast();\"},{\"path\":\"src/components/performance/MinutesSubPanel.jsx\",\"old\":\"    await generateMinutesPdf({\",\"new\":\"    try {\\n    assertExportAllowed(can,\\\"/performance/minutes\\\");\\n    await generateMinutesPdf({\"},{\"path\":\"src/components/performance/MinutesSubPanel.jsx\",\"old\":\"      squad: activeSquad,\\n    });\",\"new\":\"      squad: activeSquad,\\n    });\\n    } catch(error) {toast({title:error.message||\\\"No se pudo exportar el informe\\\",variant:\\\"destructive\\\"});}\"},{\"path\":\"src/components/nutrition/NutritionDashboard.jsx\",\"old\":\"import * as XLSX\",\"new\":\"import { useWorkspace } from \\\"@/lib/WorkspaceContext\\\";\\nimport { useToast } from \\\"@/components/ui/use-toast\\\";\\nimport { assertExportAllowed } from \\\"@/lib/exports/exportSecurity\\\";\\nimport { buildExportFileName } from \\\"@/lib/exports/pdfExportKit\\\";\\nimport * as XLSX\"},{\"path\":\"src/components/nutrition/NutritionDashboard.jsx\",\"old\":\"export default function NutritionDashboard({ players, assessments, interpretations, activeSquad }) {\",\"new\":\"export default function NutritionDashboard({ players, assessments, interpretations, activeSquad }) {\\n  const {clubBrand,can}=useWorkspace();\\n  const {toast}=useToast();\"},{\"path\":\"src/components/nutrition/NutritionDashboard.jsx\",\"old\":\"  function handleExport() {\",\"new\":\"  function handleExport() {\\n    try {\\n    assertExportAllowed(can,\\\"/performance/nutrition\\\");\"},{\"path\":\"src/components/nutrition/NutritionDashboard.jsx\",\"old\":\"    XLSX.writeFile(workbook, `nutricion-${activeTab}-${moment().format(\\\"YYYYMMDD-HHmm\\\")}.xlsx`);\",\"new\":\"    XLSX.utils.book_append_sheet(workbook,XLSX.utils.aoa_to_sheet([[\\\"Club\\\",clubBrand?.name||\\\"Club\\\"],[\\\"Plantel\\\",activeSquad?.name||\\\"Plantel\\\"],[\\\"Informe\\\",tabLabel],[\\\"Estado\\\",statusFilter],[\\\"Búsqueda\\\",search],[\\\"Generado\\\",moment().format(\\\"YYYY-MM-DD HH:mm\\\")],[\\\"Registros\\\",output.length]]),\\\"Contexto\\\");\\n    XLSX.writeFile(workbook,buildExportFileName(\\\"Nutricion\\\",[clubBrand?.name,activeSquad?.name,activeTab,moment().format(\\\"YYYYMMDD-HHmm\\\")],\\\"xlsx\\\"));\\n    }catch(error){toast({title:error.message||\\\"No se pudo exportar\\\",variant:\\\"destructive\\\"});}\"}]")
files={}
for e in fixes:
 p=e['path'];s=files.get(p,Path(p).read_text())
 assert e['old'] in s, (p,e['old'][:100])
 files[p]=s.replace(e['old'],e['new']) if e.get('all') else s.replace(e['old'],e['new'],1)
for p,s in files.items():Path(p).write_text(s)
print('Updated',len(files),'files')
